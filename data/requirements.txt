# Dépendances Python du projet SmartGamble
# Installer avec : pip install -r requirements.txt

# Client HTTP standard — utilisé par fetch_odds.py, check_bookmakers.py
requests>=2.31.0

# Client HTTP avec fingerprint TLS Chrome — bypass Cloudflare sur SofaScore
# Remplace requests standard : from curl_cffi import requests
curl_cffi>=0.7.0

# Client HTTP asynchrone — utilisé par fetch_stats.py via understat
aiohttp>=3.9.0

# Client Supabase officiel pour Python
supabase>=2.0.0

# Chargement des variables d'environnement depuis le fichier .env
python-dotenv>=1.0.0

# Librairie Understat pour récupérer les stats joueurs (xG, buts, passes)
understat>=1.0.0

# Automatisation navigateur — warmup Cloudflare (cf_clearance) pour les IPs CI/AWS
playwright>=1.40.0
